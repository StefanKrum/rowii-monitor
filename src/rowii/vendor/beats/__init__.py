"""Vendored BEATs model code (see `modules.py`/`backbone.py`/`BEATs.py` headers).

Not part of `rowii`'s own fresh-code surface: this package holds an
unmodified-in-substance copy of Microsoft's official BEATs implementation
(MIT-licensed), imported only by `rowii.signals.beats_model`. See that
module's docstring for why vendoring was chosen over a from-scratch
reimplementation.
"""
from __future__ import annotations
